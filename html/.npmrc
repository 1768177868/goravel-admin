# 确保 lock 文件包含各平台 optional 依赖，Cloudflare/Linux 上 npm ci 才能通过
include=optional

# 若 CI 报 package-lock.json 与 package.json 不同步，在 html/ 目录执行：
# Remove-Item -Recurse node_modules; Remove-Item package-lock.json; npm install --include=optional

