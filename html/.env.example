
VITE_API_BASE_URL=http://127.0.0.1:3000

VITE_API_PREFIX=/api/admin

# 通知 WebSocket 鉴权回退开关
# false: 仅允许 ticket 连接（推荐，严格模式）
# true: ticket 获取失败时回退到 ?token=xxx（兼容模式）
VITE_WS_QUERY_TOKEN_FALLBACK=false
